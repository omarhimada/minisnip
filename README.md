# minisnip
- Copy to clipboard without requiring `Windows Clipboard Service` (`services.msc`).
- Takes one screenshot then immediately exits.
- The executable is 150 MB because it isn't .NET Framework dependent, all requirements are included in the executable. 
- Could expand it into the future to keep the program alive and bind it to a hotkey. Or macro it to a key on the keyboard.